# CLAUDE.md — JackBot 行為契約

> 規則總數：12 條（Karpathy 4 條基礎 + 8 條 Agent 補丁）
> 本檔案不超過 200 行，超過後遵守率會明顯下降。

---

## 規則 1：編碼前先思考

- 不要默默做假設。要說明你的假設，暴露權衡點。
- 在猜測之前先提問。
- 當存在更簡單的方案時，主動提出反對意見。

---

## 規則 2：簡單優先

- 用能解決問題的最少程式碼。
- 不要加入想像中的功能。
- 不要為一次性程式碼設計抽象層。
- 如果一位資深工程師會認為它過度複雜，就應該簡化。

---

## 規則 3：外科手術式修改

- 只改必須改的部分。
- 不要順手「最佳化」相鄰程式碼、註釋或格式。
- 不要重構沒有壞掉的東西。
- 保持與現有風格一致。

---

## 規則 4：以目標為導向執行

- 先定義成功標準，然後迴圈迭代，直到完成驗證。
- 不要逐步指示 Claude 每一步怎麼做，而是告訴它成功結果應該是什麼樣，讓它自己迭代。

---

## 規則 5：用 AI 做對的事

- **可以用 AI 處理**：分類、起草、總結、從非結構化文本中提取資訊。
- **不要用 AI 處理**：路由、重試、狀態碼判斷、確定性轉換。
- 如果一個狀態碼已經回答了問題，讓確定性程式碼來回答，不要讓模型來決定。

---

## 規則 6：Token 預算上限

- 單個任務預算：**4,000 tokens**。
- 單次會話預算：**30,000 tokens**。
- 如果一個任務接近預算上限，總結當前狀態，然後重新開始。不要硬撐著繼續。
- 明確暴露預算超限問題，比默默超支更好。

---

## 規則 7：解決模式衝突，不要折中混用

- 如果程式碼庫中已有的兩種模式相互矛盾，不要把它們混在一起。
- 選擇其中一種模式，優先選擇更新的或經過更多測試的模式，說明理由。
- 標記另一種模式後續需要清理。
- 試圖同時滿足兩套規則的「平均程式碼」是最糟糕的程式碼。

---

## 規則 8：寫之前先讀

- 在一個檔案裡新增程式碼之前，先閱讀這個檔案的匯出內容、直接呼叫方，以及任何明顯相關的共享工具函式。
- 如果你不理解現有程式碼為什麼這樣組織，先提問，不要直接往裡加東西。
- 「在我看來這不相關」，是這個程式碼庫裡最危險的一句話。

---

## 規則 9：測試要驗證業務邏輯，不只是驗證執行

- 每一個測試都必須編碼「為什麼這個行為重要」，而不只是「它做了什麼」。
- 像 `expect(result).toBe('something')` 這樣的測試，如果函式接收的是硬編碼輸入，就沒有價值。
- 如果你寫不出一個會在業務邏輯變化時失敗的測試，那這個函式本身就是錯的。

---

## 規則 10：多步驟任務必須有檢查點

- 在多步驟任務中，每完成一個步驟後，總結已經做了什麼、驗證了什麼、還剩下什麼。
- 不要從一個你無法向我複述清楚的狀態繼續往下做。
- 如果你發現自己已經跟丟了，就停下來，重新陳述當前狀態。

---

## 規則 11：風格一致性優先於個人偏好

- 如果程式碼庫使用 snake_case，就使用 snake_case。
- 如果程式碼庫有既定的錯誤處理模式，就沿用，不要引入第二種。
- 不同意見是另一場討論。在程式碼庫內部，一致性優先於個人偏好。
- 如果你真的認為某個約定有害，就明確提出，不要默默開出一條分叉路徑。

---

## 規則 12：明確暴露不確定性，不要偽裝成功

- 如果你不能確定某件事已經成功，就明確說出來。
- 如果有記錄被靜默跳過，就不能說「完成」。
- 如果你跳過了任何測試，就不能說「測試透過」。
- 如果你沒有驗證要求的邊界情況，就不能說「功能可用」。
- 預設暴露不確定性，而不是隱藏它。

---

## 本專案專屬脈絡

**技術棧**：Python 3.11、Flask、pandas、yfinance、CoinGlass API、Telegram Bot API、Discord Webhook、Docker Compose

**主程式**：`jackbot.py`（~19,000 行，含 14 支訊號模組）

**新增訊號**：加在 `jackbot.py` 末端的 `if __name__ == "__main__"` 區塊，並同步更新 `app.py` 的端點與 `.github/workflows/` 的對應 yml。

**既有模式**（選一，不要混用）：
- 訊號推播：`send_telegram_message(text, TG_THREAD_IDS["<key>"], parse_mode="Markdown")`
- 冷卻狀態：`data/` 目錄下的 JSON 檔，跨 session 靠 GitHub Gist 持久化
- 錯誤處理：`try/except` + `logger.error` + 若嚴重則發 TG 警告訊息

**部署流程**：
1. 修改 `jackbot.py` / `app.py`
2. `git push` → GitHub Actions 自動觸發；或 `docker compose build jackbot && docker compose up -d jackbot`（Vultr 伺服器）

**絕對不要動**：`_calc_signal_grade()`、`_crit_radar_score_components()`、`jackbot_universal_pre_send_gatekeeper()` 的核心評分邏輯，除非明確指示。
