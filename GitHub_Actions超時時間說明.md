# GitHub Actions 超時時間說明

## ✅ 好消息：不會有 30 秒超時問題！

**GitHub Actions 的默認超時時間**：
- **公開倉庫**：**6 小時**（360 分鐘）
- **私有倉庫**：**6 小時**（免費版）

**與 cron-job.org 的對比**：
- cron-job.org 免費版：30 秒超時 ❌
- GitHub Actions：6 小時超時 ✅

---

## 📊 當前超時設置

我已為每個 workflow 設置了超時時間：

| Workflow | 當前超時時間 | 說明 |
|---------|------------|------|
| 主流板塊排行榜推播 | 10 分鐘 | ✅ 足夠 |
| 巨鯨持倉動向 | 10 分鐘 | ✅ 足夠 |
| **持倉變化篩選** | **30 分鐘** | ⚠️ 處理 904 個幣種，可能需要更長時間 |
| 重要經濟數據推播 | 15 分鐘 | ✅ 足夠 |
| 新聞快訊推播 | 10 分鐘 | ✅ 足夠 |
| 資金費率排行榜 | 10 分鐘 | ✅ 足夠 |
| 長線牛熊導航儀 | 10 分鐘 | ✅ 足夠 |
| 流動性獵取雷達 | 15 分鐘 | ✅ 足夠 |
| 山寨爆發雷達 | 15 分鐘 | ✅ 足夠 |

---

## ⚠️ 如果「持倉變化篩選」執行時間超過 30 分鐘

如果發現「持倉變化篩選」執行失敗（超時），可以增加超時時間：

### 修改方法：

編輯 `.github/workflows/position-change.yml` 文件：

**找到這行**：
```yaml
timeout-minutes: 30
```

**改為**：
```yaml
timeout-minutes: 60  # 或更長時間，最多 360 分鐘（6小時）
```

**建議時間**：
- 如果 30 分鐘不夠 → 改為 `60` 分鐘
- 如果 60 分鐘不夠 → 改為 `120` 分鐘（2小時）
- 如果需要更長時間 → 可以設置到 `360` 分鐘（6小時）

---

## 🔧 如何修改超時時間

### 步驟 1：編輯 Workflow 文件

1. 在 GitHub 倉庫中，進入 `.github/workflows/` 目錄
2. 找到要修改的 workflow 文件（例如：`position-change.yml`）
3. 點擊「Edit」（編輯）按鈕

### 步驟 2：修改 timeout-minutes

找到：
```yaml
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 30  # 修改這裡的數字
```

改為您需要的時間（例如：`60`、`120`、`180` 等）

### 步驟 3：提交更改

1. 在底部輸入提交訊息（例如：`Increase timeout for position change`）
2. 點擊「Commit changes」

---

## ✅ 當前設置應該足夠

根據您的需求：
- 大部分任務：10-15 分鐘超時 ✅ 足夠
- 持倉變化篩選：30 分鐘超時 ⚠️ 應該足夠，但如果不夠可以增加到 60 分鐘

**GitHub Actions 不會像 cron-job.org 那樣 30 秒就停止，最少也有 10 分鐘，最多可以設置到 6 小時！**

---

## 📋 檢查是否超時

### 如何知道是否超時？

在執行日誌中，如果超時會顯示：
- `The job has exceeded the maximum time limit for jobs`
- 或者執行狀態顯示「Timeout」或「Cancelled」

### 如果發現超時：

1. **增加 timeout-minutes**（按照上面的方法）
2. **或者優化代碼執行效率**（減少不必要的等待時間）

---

## 🎯 建議

**當前設置應該足夠**，因為：
- GitHub Actions 默認就有 6 小時超時
- 已為每個 workflow 設置了合理的超時時間
- 持倉變化篩選已設置為 30 分鐘（處理 904 個幣種）

**如果未來發現超時問題**，再按照上面的方法增加超時時間即可。

---

## ✅ 結論

**您不需要擔心超時問題！**

- ✅ GitHub Actions 默認 6 小時超時（遠超 cron-job.org 的 30 秒）
- ✅ 已為每個 workflow 設置了合適的超時時間
- ✅ 如果未來需要，可以輕鬆增加超時時間

**現在什麼都不用做，當前設置已經足夠！** 🎉


