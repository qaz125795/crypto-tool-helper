# CoinGlass 標準版 → 初創版 降版影響說明

根據 [CoinGlass 定價頁](https://www.coinglass.com/zh/pricing)：

| 項目 | 初創版 | 標準版（你目前） |
|------|--------|------------------|
| 調用限制 | **80 次/分鐘** | 300 次/分鐘 |
| 數據端點 | **80+** 個 | 90+ 個 |
| 月費 | $79/月 | $299/月 |

**全專案已做**：所有推播工具的 CoinGlass 請求皆已加入 80 次/分鐘限速，降版後**不需刪除任何工具**，只需在 GitHub 設 Variable（見下文）。完整對照見 **《推播工具_CoinGlass_API對照表.md》**。

---

## 一、持倉變化篩選（15 分鐘循環）— 已支援初創版限速，不必刪除

- **使用端點**：`/api/futures/coins-price-change`（1 次）、`/api/futures/supported-exchange-pairs`（1 次）、**`/api/futures/open-interest/history`（每個幣種 1 次）**。
- **初創版 80 次/分鐘**：已內建**限速模式**，可**繼續全合約覆蓋**，單輪約 **7–8 分鐘**跑完（567 幣種 ÷ 80 ≈ 7.1 分鐘），不影響「每 15 分鐘抓持倉異常」的策略。

**啟用方式（初創版必做）**：

1. 到 GitHub 倉庫 → **Settings** → **Secrets and variables** → **Actions** → 分頁 **Variables**。
2. 新增變數：名稱 `COINGLASS_RATE_LIMIT_PER_MINUTE`，值 `80`。
3. 儲存。之後每次持倉變化任務會自動限速、覆蓋全幣種，單輪約 7–8 分鐘內完成並推播。

（未設定或設為 0 時，維持原本高並發、約 10 秒內跑完，適合標準版；標準版若常遇到 OI 失敗，也可試設 80 減輕限流。）

---

## 二、其餘功能概估（單獨跑多半可正常）

以下為各功能單次執行時，對 CoinGlass 的**約略請求次數**。初創版 80 次/分鐘下，只要**不要多個功能在同一分鐘內一起跑**，多數可以正常運作。

| 功能 | 約略 CoinGlass 請求次數/次 | 說明 |
|------|---------------------------|------|
| 購買力監控 | 約 2 | 穩定幣市值 + 聚合 OI，可保留。 |
| 巨鯨/多空比（購買力或舊版巨鯨） | 約 3～9 | 全球/大戶多空比等，可保留。 |
| 重要經濟數據推播 | 約 3 | 經濟數據 + 財經事件 + 央行活動，可保留。 |
| 新聞快訊（CoinGlass 部分） | 約 2 | article + newsflash，可保留。 |
| 資金費率排行榜 | 約 1 | 可保留。 |
| 長線牛熊導航儀 | 約 4 | Ahr999、彩虹圖、Pi、恐懼貪婪，可保留。 |
| 流動性獵取雷達 | 約 3 | 僅 BTC/ETH/SOL 清算，可保留。 |
| 山寨爆發雷達 | 約 10～40 | altcoin-season + RSI 列表 + 部分幣種 orderbook/OI，可保留；若遇 429 可考慮拉長執行間隔。 |
| Hyperliquid 聰明錢監控 | 約 3 | whale-alert + pnl + whale-position，可保留。 |

注意：初創版為 **80+ 端點**，標準版為 **90+ 端點**。若降版後某功能出現「端點不存在」或 403/404，代表該端點可能僅在標準版提供，屆時再考慮停用該功能。

---

## 三、建議操作清單（降版後）

1. **持倉變化篩選**  
   - 在 GitHub **Variables** 新增 `COINGLASS_RATE_LIMIT_PER_MINUTE` = `80`（見上一節）。  
   - 排程與外部觸發**照常使用**，無需停用；單輪約 7–8 分鐘、全幣種覆蓋。

2. **其餘推播**  
   - 保留其餘功能，但盡量**錯開排程**（例如持倉以外的不要都擠在同一分鐘），避免多個 workflow 同時打 CoinGlass，合計超過 80 次/分鐘。

3. **觀察幾天**  
   - 若某個功能開始出現 **429 Too Many Requests** 或限流相關錯誤，可：  
     - 拉長該功能的執行間隔，或  
     - 暫時停用該功能。

4. **若遇「端點不可用」**  
   - 記下是哪一個 API 路徑（例如 `/api/xxx/yyy`），對照 CoinGlass 文件或詢問客服，確認是否僅標準版提供；若是，該功能需停用或改寫。

---

## 四、總結

| 項目 | 建議 |
|------|------|
| **持倉變化篩選** | **不必刪除**。設 Variables `COINGLASS_RATE_LIMIT_PER_MINUTE` = `80`，即全幣種覆蓋、單輪約 7–8 分鐘。 |
| **其餘推播工具** | 可保留；注意錯開執行時間，避免疊加超過 80 次/分鐘。 |
| **端點差異** | 若某功能報錯「端點不存在」或權限錯誤，再考慮停用該功能。 |

降版後持倉變化改為限速模式即可保留全合約策略，其餘功能依上表控制好頻率即可。
