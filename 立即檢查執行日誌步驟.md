# 立即檢查執行日誌步驟

## 🚨 請現在立即執行以下步驟：

### 步驟 1：查看執行日誌

1. **前往 GitHub Actions 頁面**
2. **點擊任意一個執行記錄**（例如：「主流板塊排行榜推播」）
3. **點擊「Run sector ranking task」步驟**
4. **查看日誌內容**

### 步驟 2：告訴我看到什麼

請告訴我日誌最後幾行顯示了什麼：

**可能的錯誤訊息**：
- `KeyError: 'TG_TOKEN'` → Secrets 未設置
- `401 Unauthorized` → Token 或 Chat ID 錯誤
- `發送 Telegram 訊息失敗` → Telegram API 錯誤
- `Telegram 訊息發送成功` → 應該收到訊息但沒收到（可能是 Thread ID 問題）

### 步驟 3：檢查 Secrets

同時請確認：
1. **Settings → Secrets and variables → Actions**
2. **是否有以下 5 個 Secrets**：
   - `TG_TOKEN`
   - `CHAT_ID`
   - `CG_GECKO_API_KEY`
   - `CG_API_KEY`
   - `TG_THREAD_SECTOR_RANKING`（或對應的 Thread ID）

---

## 🔍 快速診斷

**如果日誌中看到**：
- ✅ `Telegram 訊息發送成功` → 訊息已發送，檢查 Telegram 的 Thread ID 是否正確
- ❌ `KeyError` 或 `None` → Secrets 未設置或名稱錯誤
- ❌ `401` 或 `403` → Token 或 Chat ID 錯誤
- ❌ `發送失敗` → 檢查 Telegram Bot 權限或網絡問題

---

**請立即查看執行日誌，並告訴我看到了什麼錯誤訊息！**


