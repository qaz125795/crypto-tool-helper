# 用外部定時「準時」觸發推播（每 15／10 分鐘）

## 為什麼需要這個？

GitHub Actions 的 **schedule（cron）對短週期不可靠**：  
設定每 15 分鐘或每 10 分鐘時，實際觸發常會延遲到 30 分鐘、1 小時甚至 2 小時才跑一次，這是 GitHub 端的限制，不是設定錯誤。

若要**真正每 15 分鐘／每 10 分鐘**觸發一次，必須用**外部定時服務**每 15／10 分鐘打一次 GitHub API，觸發 `workflow_dispatch`。

---

## 前置：取得 GitHub Personal Access Token (PAT)

1. 登入 GitHub → 右上頭像 → **Settings** → 左側 **Developer settings** → **Personal access tokens** → **Tokens (classic)**。
2. **Generate new token (classic)**，Note 填例如 `trigger-workflows`。
3. 勾選權限：**repo**（或至少 **repo:status** 與能觸發 Actions 的權限；若用 fine-grained token，需 **Actions: Write**）。
4. 產生後**複製並妥善保存**（只顯示一次），下面會當成 `你的PAT` 使用。

---

## 一、持倉變化篩選（每 15 分鐘一次）

### 使用 cron-job.org

1. 前往 [cron-job.org](https://cron-job.org) 並登入。
2. **Create cronjob**：
   - **Title**：`JackBot 持倉變化 15min`
   - **URL**：  
     `https://api.github.com/repos/你的用戶名/你的倉庫名/actions/workflows/position-change.yml/dispatches`  
     例如倉庫是 `qaz125795/JackBot` 則為：  
     `https://api.github.com/repos/qaz125795/JackBot/actions/workflows/position-change.yml/dispatches`
   - **Method**：`POST`
   - **Request Headers**（新增兩筆）：
     - `Authorization`：`token 你的PAT`
     - `Accept`：`application/vnd.github.v3+json`
   - **Request Body**：選 **Raw body**，內容：  
     `{"ref":"main"}`
   - **Schedule**：選 **Every 15 minutes**（或自訂 cron：`*/15 * * * *`，即每 15 分鐘）。
3. 儲存並啟用。

### 用 curl 手動測試（本機）

```bash
curl -X POST \
  -H "Authorization: token 你的PAT" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/repos/你的用戶名/你的倉庫名/actions/workflows/position-change.yml/dispatches \
  -d '{"ref":"main"}'
```

若成功，HTTP 狀態為 204，且 Actions 頁面會出現一次「持倉變化篩選」的 run（觸發方式為 workflow_dispatch）。

---

## 二、Hyperliquid 聰明錢監控（每 10 分鐘一次）

### 使用 cron-job.org

1. 再 **Create cronjob** 一個：
   - **Title**：`JackBot Hyperliquid 10min`
   - **URL**：  
     `https://api.github.com/repos/你的用戶名/你的倉庫名/actions/workflows/hyperliquid.yml/dispatches`
   - **Method**：`POST`
   - **Request Headers**：同上（`Authorization`、`Accept`）
   - **Request Body**：`{"ref":"main"}`
   - **Schedule**：**Every 10 minutes**（或 cron：`*/10 * * * *`）。
2. 儲存並啟用。

### 用 curl 手動測試

```bash
curl -X POST \
  -H "Authorization: token 你的PAT" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/repos/你的用戶名/你的倉庫名/actions/workflows/hyperliquid.yml/dispatches \
  -d '{"ref":"main"}'
```

---

## 注意事項

- **用戶名／倉庫名**：請改成你實際的 GitHub 用戶名與倉庫名（例如 `qaz125795/JackBot`）。
- **分支**：上面用 `main`；若預設分支是 `master`，把 Body 改成 `{"ref":"master"}`。
- **PAT 安全**：不要在程式碼或公開地方寫入 PAT；cron-job.org 會加密儲存。
- **保留 schedule**：workflow 裡可繼續保留 `schedule`，當作 GitHub 有跑時的備援；外部觸發為主即可。

完成上述設定後，持倉變化會由外部**每 15 分鐘**、Hyperliquid **每 10 分鐘**準時觸發一次，不受 GitHub schedule 延遲影響。
