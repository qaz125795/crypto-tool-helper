# 推播準時性說明

## 為什麼有時會延遲？

1. **GitHub Actions 排程不保證準點**  
   使用 `schedule`（cron）時，GitHub 會在「大約」該時間觸發，可能晚幾分鐘甚至更久，尤其平台忙碌時。

2. **之前設成「取消進行中任務」**  
   若新一次觸發時，上一次還在跑，舊任務會被取消，導致那次的推播永遠不會送出，看起來就像間隔變長（例如 30～50 分鐘才推一次）。

## 我們已做的優化

### 持倉變化篩選（15 分鐘一次）

- **8 分鐘內強制結束**：腳本最多跑 8 分鐘就會結束並推播，避免拖到下一輪。
- **不取消進行中任務**：每次觸發都會跑完並推播，不會再因「新的一輪開始」而把上一輪取消。
- **排程錯開 1 分鐘**：改為每小時的 **01、16、31、46 分**（UTC）觸發，減少和其他 workflow 同時搶 runner。
- **並行數提高**：由 20 改為 35，並將單一 API 請求 timeout 改為 6 秒，加快整體完成時間。

### 其餘推播工具

- 所有具排程的 workflow 都已改為 **不取消進行中任務**（`cancel-in-progress: false`），  
  每次觸發都會盡量跑完並推播，減少漏推或間隔過長。

## 若仍希望「更準點」推播

GitHub 的 `schedule` 無法保證分秒不差，若你希望更接近「每 15 分鐘整點」推播，可以：

1. **用外部定時服務觸發**  
   使用 [cron-job.org](https://cron-job.org) 或類似服務，每 15 分鐘打一次 GitHub API，觸發「持倉變化」的 `workflow_dispatch`：
   - 方法：`POST`  
   - URL：`https://api.github.com/repos/你的用戶名/你的倉庫名/actions/workflows/position-change.yml/dispatches`  
   - Body：`{"ref":"main"}`  
   - Header：`Authorization: token 你的PAT`、`Accept: application/vnd.github.v3+json`

2. **用 Zeabur 等常駐服務**  
   若你有用 Zeabur 部署同一個專案，可在上面用 cron 或內部排程每 15 分鐘呼叫一次對應 API，由伺服器端控制時間，通常會比 GitHub Actions 的 schedule 更準。

## 總結

- 持倉變化：**8 分鐘內完成並推播**、**不取消進行中任務**、排程錯開，預期可改善「30～50 分鐘才推一次」的狀況。
- 其他推播工具：改為不取消進行中任務，每次觸發都有機會完整跑完並推播。
- 若仍覺得時間不夠準，可搭配**外部定時觸發**或 **Zeabur 排程** 使用。
