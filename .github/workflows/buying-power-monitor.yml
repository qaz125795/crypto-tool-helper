name: 購買力監控（穩定幣市值 + OI 監控）

on:
  schedule:
    # 每 4 小時執行一次（UTC 時間：00:00, 04:00, 08:00, 12:00, 16:00, 20:00）
    - cron: '0 */4 * * *'
  workflow_dispatch:

# 防止並發執行
concurrency:
  group: buying-power-monitor
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      actions: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run buying power monitor task
        env:
          CG_API_KEY: ${{ secrets.CG_API_KEY }}
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          TG_THREAD_IDS: ${{ secrets.TG_THREAD_IDS }}
          TG_THREAD_WHALE_POSITION: ${{ secrets.TG_THREAD_WHALE_POSITION }}
        run: |
          timeout 480 python jackbot.py buying_power_monitor || exit 0
          echo "任務執行完成"
